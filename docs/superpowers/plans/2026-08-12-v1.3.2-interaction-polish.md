# v1.3.2 Interaction Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the site-scope override bug and ship a professional interval Stepper plus edge-anchored, draggable Mini Timer.

**Architecture:** Keep the existing v2 rule schema and deadline timer unchanged. Add pure helpers for interval stepping and safer site-rule defaults, improve the settings dialog markup/interaction, then refactor panel mode transitions so geometry is anchored to the nearest viewport edges and Mini has its own drag gesture.

**Tech Stack:** Vanilla JavaScript userscript, Shadow DOM, GM storage APIs, Node `node:test`, Chrome headless HTTP smoke harness.

---

### Task 1: Lock the site-scope bug and Stepper behavior with tests

**Files:**
- Modify: `tests/smart-auto-refresh-pro.test.js`
- Modify: `smart-auto-refresh-pro.user.js`

- [ ] Add a regression asserting a site update with an existing exact rule removes that exact rule when no override decision is explicitly supplied.
- [ ] Add tests for a pure `stepIntervalValue(value, direction, step)` helper: increment, decrement, min=1 clamp, 10-second step, invalid current value fallback.
- [ ] Run `node --test tests/smart-auto-refresh-pro.test.js` and verify the new tests fail before implementation.
- [ ] Implement the minimal pure helpers/default semantics.
- [ ] Re-run tests and require zero failures.

### Task 2: Build the professional refresh-interval Stepper

**Files:**
- Modify: `smart-auto-refresh-pro.user.js`
- Modify: `tests/browser-smoke.html`

- [ ] Replace the plain interval field with `− / input / +` controls while preserving direct numeric entry.
- [ ] Add ArrowUp/ArrowDown and mouse-wheel handling; Shift changes the step from 1 to 10 seconds.
- [ ] Add press-and-hold repeat for the buttons after ~350ms, with cleanup on pointerup/pointercancel/lost capture.
- [ ] Keep the existing 1–4 second high-frequency confirmation unchanged.
- [ ] Extend Chrome smoke to verify click, wheel, keyboard, and min clamp behavior.

### Task 3: Rewrite scope cards and fix conflict UX

**Files:**
- Modify: `smart-auto-refresh-pro.user.js`
- Modify: `tests/browser-smoke.html`

- [ ] Rename cards to “仅当前页面” and “整个网站”, with small semantic badges and concrete descriptions.
- [ ] Display current full URL/hostname in the appropriate card.
- [ ] Change the conflict panel to default to “使用网站规则（推荐）”, explaining that the current-page override will be removed and the new rule will immediately affect this page.
- [ ] Keep “保留当前页单独规则” as the opt-in advanced alternative.
- [ ] Change save fallback from `keep` to `remove`.
- [ ] Extend Chrome smoke to assert the recommended conflict option is selected by default.

### Task 4: Refactor Mini Timer dragging and edge anchoring

**Files:**
- Modify: `smart-auto-refresh-pro.user.js`
- Modify: `tests/browser-smoke.html`

- [ ] Remove Mini hover auto-expand; hover only affects visual emphasis.
- [ ] Implement Mini pointer gesture discrimination: movement beyond a small threshold drags; release without movement expands.
- [ ] Add geometry helpers that capture nearest horizontal/vertical edge gaps before a mode change and restore the same anchor after width/height changes.
- [ ] Use Mini dimensions while dragging Mini, so it can reach the true right edge.
- [ ] Keep expanded header dragging and stored position compatibility.
- [ ] Extend Chrome smoke to verify Mini can be moved to the right edge, remains Mini during hover/drag, and keeps the right edge when expanded.

### Task 5: Release gate and docs

**Files:**
- Modify: `smart-auto-refresh-pro.user.js`
- Modify: `README.md`
- Modify: `tests/smart-auto-refresh-pro.test.js`

- [ ] Run Node tests, `node --check`, and `git diff --check` while metadata remains v1.3.1.
- [ ] Run HTTP Chrome smoke and require all interaction assertions to pass.
- [ ] Only after all gates pass, bump `@version` to `1.3.2` and update metadata test.
- [ ] Update README with Stepper controls, clearer scope semantics, site-rule takeover behavior, and Mini drag/edge-anchor behavior.
- [ ] Re-run all release gates.
- [ ] Commit, push `main`, fast-forward the retained `/Volumes/980 Pro 500G/Users/jingchen/Github/smart-auto-refresh-pro` checkout, rerun tests there, then remove the temporary checkout and restore the previous GitHub account.
